print "Testing Standard Library Auto-load";

print "1. Testing List";
let l = List();
l.push(10);
l.push(20);
l.push(30);

print "List items:";
l.forEach(fn(x) { 
  print x; 
});

let doubled = l.map(fn(x) { return x * 2; });
print "Doubled items:";
doubled.forEach(fn(x) { 
  print x; 
});

print "2. Testing Math";
print "Max(10, 20): " + Math.max(10, 20);
print "Pow(2, 3): " + Math.pow(2, 3);

print "3. Testing FSK Lib (still here?)";
print "Random: " + FSK.random(1, 100);
