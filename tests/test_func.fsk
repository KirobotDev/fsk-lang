fn sayHello(name) {
    print "Hello, " + name + "!"
}

fn add(a, b) {
    print "Adding " + a + " and " + b
    return a + b
}

sayHello("FSK User")

let result = add(10, 20)
print "Result of addition: " + result
print "Show Allways: Features " 

print "Current time (clock): " + clock()

fn countdown(n) {
    if (n > 0) {
        print n
        countdown(n - 1)
    } else {
        print "Blast off!"
    }
}

print "Starting countdown..."
countdown(3)
