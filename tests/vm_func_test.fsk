print "--- VM Function & Recursion Test ---";

VM.run("
fn add(a, b) {
    return a + b;
}

fn fib(n) {
    if (n < 2) return n;
    return fib(n - 1) + fib(n - 2);
}

print \"10 + 20 = \";
print add(10, 20);

print \"Fibonacci(7) = \";
print fib(7);
");

print "VM Function Test Complete.";
