print "--- Test Event Loop ---";

setTimeout(() => {
    print "Message après 500ms";
}, 500);

setTimeout(() => {
    print "Message après 100ms";
}, 100);

let count = 0;
let id = setInterval(() => {
    count = count + 1;
    print "Intervalle : " + count;
    if (count == 3) {
        print "Fin des intervalles (simulation)";
    }
}, 200);

print "Fin du script principal (mais l'event loop continue)";
